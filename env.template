# Football Fan AI Agent Environment Configuration
# Copy this file to .env and fill in your actual API keys

# DeepSeek API Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Football Data API Configuration (for scheduler jobs)
FOOTBALL_DATA_API_KEY=your_football_data_api_key_here
FOOTBALL_DATA_API_BASE_URL=https://api.football-data.org/v4

# Optional: Other API keys (legacy support)
FOOTBALL_API_KEY=your_football_api_key_here

# Google Calendar Configuration
# Choose ONE authentication method below:

# Method 1: OAuth 2.0 (Interactive - requires browser)
# Note: You need to download credentials.json from Google Cloud Console
# The token.json file will be created automatically after first authentication
GOOGLE_CALENDAR_CREDENTIALS_PATH=credentials.json
GOOGLE_CALENDAR_TOKEN_PATH=token.json

# Method 2: Service Account (Automated - no browser interaction)
# Download service account key from Google Cloud Console
# GOOGLE_CALENDAR_SERVICE_ACCOUNT_PATH=service-account-key.json

# Method 3: Application Default Credentials (ADC)
# Uses gcloud CLI credentials or GCP metadata service
# GOOGLE_CALENDAR_USE_ADC=true

# Method 4: API Key (Read-only access only)
# Simple key-based authentication - cannot create/modify events
# GOOGLE_CALENDAR_API_KEY=your_google_api_key_here

# Scheduler Configuration
# The scheduler runs jobs at specific times:
# - Database loading: Every Monday at 10:30 AM
# - You can modify these schedules in scheduler.py

# Database Configuration
# The db/ directory will be automatically created and mounted
# All data will be persisted in this directory

# Logging Configuration
# Logs are output to stdout/stderr for Docker compatibility
# Set LOG_LEVEL=DEBUG for more verbose logging if needed
LOG_LEVEL=INFO
